# lgdx_backend 프로젝트 개요

## 한 줄 소개

`lgdx_backend`는 `lgdx_frontend`와 함께 동작하는 하나의 서비스형 프로젝트에서 백엔드 역할을 담당하는 저장소입니다.

이 프로젝트는 사용자의 질문, 실시간 상호작용, 매뉴얼 기반 검색, 이미지/영상 생성 흐름을 하나로 묶어 제품 지원 경험을 만드는 것을 목표로 합니다.

## 이 저장소의 역할

이 저장소는 전체 프로젝트에서 아래 기능을 담당합니다.

- Flutter 앱에서 들어오는 요청을 처리하는 서버 레이어
- 매뉴얼 기반 검색과 답변 생성을 담당하는 RAG 레이어
- Firestore, Supabase, Firebase Storage 등 외부 서비스 연동
- 이미지/영상 생성 파이프라인 실행 및 결과 등록
- 비전 입력이나 후처리 로직을 묶는 백엔드 유틸리티

즉, `lgdx_frontend`가 사용자 경험을 담당한다면 `lgdx_backend`는 데이터, 추론, 생성, 저장 흐름을 연결하는 실행 엔진에 가깝습니다.

## 전체 프로젝트 관점에서의 구조

전체 프로젝트는 크게 두 저장소로 나뉩니다.

- `lgdx_frontend`
  사용자가 직접 보는 Flutter 앱과 화면 흐름
- `lgdx_backend`
  검색, 답변 생성, 미디어 생성, 데이터 저장을 처리하는 서버 및 파이프라인

사용자 입장에서는 하나의 앱처럼 보이지만, 실제 구현은 프론트와 백엔드를 분리해 역할을 나눈 구조입니다.

## 주요 기능 흐름

### 1. 질문/대화 처리

- 프론트엔드에서 사용자 질문을 전송
- 백엔드가 매뉴얼과 대화 맥락을 바탕으로 검색 수행
- 생성형 모델을 통해 답변을 구성
- 결과를 Firestore 등에 기록하여 앱 화면과 상태를 맞춤

### 2. 매뉴얼 기반 검색

- PDF 또는 문서 기반 매뉴얼 업로드
- 임베딩 생성 및 벡터 저장
- 검색 질의 확장 또는 전처리
- 관련 문맥을 바탕으로 제품 지원 답변 생성

### 3. 미디어 생성 파이프라인

- 사용자 대화나 상황 데이터를 기반으로 영상 프롬프트 생성
- OpenAI Sora 또는 Gemini/Veo 계열 생성 로직 호출
- 생성 결과를 파일로 저장
- 결과 URL 또는 메시지를 앱에서 소비할 수 있도록 등록

### 4. 비전/실시간 입력 보조

- 비전 관련 스크립트와 테스트 코드로 이미지 입력 처리 실험
- 프론트의 라이브 화면 또는 카메라 흐름과 연계 가능한 서버 보조 기능 제공

## 디렉터리 기준으로 보면

### `RAG/`

- 검색 기반 답변 서버의 핵심 영역
- FastAPI 서버, 문서 업로드, 매뉴얼 기반 검색 관련 코드 포함
- Firestore, Supabase, Gemini 연동 흐름이 이 영역에 집중됨

### `generate/`

- 이미지/영상 생성 파이프라인 관련 코드
- 프롬프트 생성, 생성 모델 호출, 결과 저장, 상태 확인 등 담당

### `vision/`

- 비전 관련 실험 및 보조 스크립트
- 향후 실시간 입력 처리나 시연용 기능 연결 지점으로 볼 수 있음

### 기타 루트 스크립트

- 환경 점검, 디버그, 스토리지 조회, 수동 테스트용 보조 코드

## 기술 스택

코드 기준으로 확인되는 핵심 스택은 아래와 같습니다.

- Python
- FastAPI
- Firebase / Firestore
- Supabase
- Gemini 계열 API
- OpenAI 기반 생성 흐름
- PDF/문서 업로드 및 임베딩 처리

## 이 저장소를 읽을 때의 권장 관점

이 저장소는 단순한 API 서버 하나보다는 다음 세 가지를 함께 가진 프로젝트로 보는 것이 맞습니다.

- 제품 지원용 RAG 백엔드
- 생성형 미디어 파이프라인
- 프론트엔드와 이어지는 상태 관리/저장 레이어

그래서 문서를 읽거나 코드를 이해할 때도 개별 스크립트보다 "사용자 요청이 들어와서 앱 화면으로 다시 돌아가기까지 어떤 데이터가 이동하는가" 관점으로 보면 구조가 더 잘 보입니다.

## 함께 봐야 하는 저장소

- `lgdx_frontend`

프론트엔드는 이 백엔드가 제공하는 API, 메시지 흐름, 미디어 결과를 실제 사용자 화면과 인터랙션으로 바꾸는 역할을 합니다.

두 저장소는 별개 프로젝트가 아니라, 하나의 서비스 구현을 프론트와 백엔드로 나눈 구성입니다.

