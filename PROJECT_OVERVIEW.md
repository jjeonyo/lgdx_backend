# lgdx_backend 프로젝트 개요

## 문서 목적

이 문서는 설치 방법이나 실행 절차를 설명하기 위한 README가 아니라, 실무진이 이 저장소를 빠르게 읽고 프로젝트 맥락과 설계 의도를 이해할 수 있도록 정리한 포트폴리오용 개요 문서입니다.

`lgdx_backend`와 `lgdx_frontend`는 서로 다른 프로젝트가 아니라, 하나의 사용자 지원 서비스를 프론트엔드와 백엔드로 분리해 관리한 결과물입니다.

## 프로젝트 한 줄 요약

사용자의 질문, 제품 매뉴얼, 실시간 상호작용, 생성형 미디어 흐름을 하나로 묶어 "제품 문제 해결 경험"을 앱 형태로 구현한 프로젝트입니다.

이 저장소는 그중에서도 검색, 추론, 생성, 저장, 외부 서비스 연동을 담당하는 백엔드 영역입니다.

## 내가 맡은 역할

이 프로젝트에서는 프론트엔드와 백엔드가 분리되어 있지만, 전체 흐름을 끊기지 않게 연결하는 관점에서 백엔드 영역을 중심적으로 다뤘습니다.

이 저장소 기준으로 제가 맡은 역할은 아래와 같습니다.

- 매뉴얼 기반 검색과 답변 생성 흐름 설계
- Firestore, Supabase 등 외부 서비스와 연결되는 서버 로직 구성
- 생성형 이미지/영상 파이프라인을 서비스 흐름에 편입
- 프론트엔드가 소비할 수 있는 응답 구조와 상태 반영 방식 정리
- 단순 API 구현이 아니라 사용자 요청에서 결과 반환까지 이어지는 전체 흐름 연결

포트폴리오 관점에서 보면, 이 저장소는 제가 "백엔드 기능 하나"보다 "AI 기능이 포함된 서비스 흐름을 서버 구조로 어떻게 풀었는지"를 보여주는 영역입니다.

## 어떤 문제를 풀고자 했는가

이 프로젝트가 겨냥한 문제는 단순 챗봇 구현이 아닙니다.

- 사용자는 제품 문제를 자연어로 설명합니다.
- 시스템은 매뉴얼 기반으로 답해야 합니다.
- 답변은 단순 텍스트를 넘어, 상황에 따라 이미지/영상 형태의 안내로 확장될 수 있어야 합니다.
- 결과는 앱 안에서 다시 소비되어야 하므로, 프론트와 백엔드 상태가 끊기지 않아야 합니다.

즉, 이 프로젝트는 "생성형 AI를 붙였다"보다 "실제 사용자 흐름 안에서 AI 기능이 어떤 구조로 동작해야 하는가"를 다룬 결과물에 가깝습니다.

## 이 저장소가 맡은 책임

`lgdx_backend`는 전체 서비스에서 아래 책임을 가집니다.

- 앱 요청을 받아 처리하는 서버 계층
- 매뉴얼 기반 검색과 답변 생성을 담당하는 RAG 계층
- Firestore, Supabase 등 상태 저장소와의 연결
- 영상 생성 파이프라인 실행 및 결과 등록
- 프론트엔드가 소비할 수 있는 형태로 결과를 정리해 반환

실무 관점에서 보면 이 저장소는 단순 API 모음이 아니라, 여러 외부 서비스와 모델 호출을 하나의 사용자 시나리오로 연결하는 orchestration layer에 가깝습니다.

## 왜 프론트와 백엔드를 분리했는가

사용자 경험과 생성/검색 파이프라인은 변경 주기와 책임이 다르기 때문에 분리하는 것이 맞다고 판단했습니다.

- 프론트엔드는 화면, 입력 흐름, 재생 경험, 상태 반영에 집중
- 백엔드는 검색, 생성, 저장, 연동, 비동기 처리에 집중

이 분리는 단순히 디렉터리를 나눈 수준이 아니라, 역할 경계를 의식하고 서비스 구조를 나눈 흔적이라고 봐주시면 됩니다.

## 이 저장소에서 보여주고 싶은 역량

### 1. AI 기능을 "서비스 흐름"으로 연결하는 능력

이 저장소는 모델 호출 자체보다, 모델 호출 전후의 맥락 관리와 결과 소비 구조를 더 중요하게 다룹니다.

- 질문 수집
- 관련 문맥 검색
- 생성형 모델 호출
- 결과 저장
- 앱에 다시 반영

이 흐름이 끊기지 않도록 구성했다는 점이 핵심입니다.

### 2. RAG를 실제 제품 문제에 맞게 붙이는 능력

`RAG/` 영역은 단순 예제 수준이 아니라, 매뉴얼 기반 답변이라는 분명한 목적을 갖고 있습니다.

- 문서 업로드
- 임베딩 생성
- 벡터 검색
- 관련 문맥을 바탕으로 응답 생성

즉, "검색이 되는지"보다 "제품 지원이라는 사용 사례에 맞는 답변을 만들 수 있는지"에 초점을 둔 구성입니다.

### 3. 생성형 미디어 파이프라인을 서비스에 녹여내는 시도

`generate/` 영역은 텍스트 답변에서 끝나지 않고, 상황에 따라 영상 안내까지 확장하려는 시도를 보여줍니다.

- 대화 맥락 기반 프롬프트 생성
- 생성 모델 호출
- 파일 저장 및 상태 반영
- 앱에서 재생 가능한 형태로 전달

이 부분은 AI 모델 호출 경험 자체보다, 생성 결과를 제품 기능으로 편입시키는 관점에서 의미가 있습니다.

### 4. 외부 서비스 연동과 상태 관리

이 저장소는 로컬 로직만으로 끝나지 않습니다.

- Firestore
- Supabase
- Firebase Storage 성격의 흐름
- 생성형 AI API

여러 시스템을 동시에 다루기 때문에, 실무에서는 장애 지점과 상태 불일치가 쉽게 생깁니다. 이 저장소는 그런 복합 구조를 직접 다뤄본 흔적이 있는 프로젝트입니다.

## 구조를 어떻게 보면 좋은가

### `RAG/`

이 프로젝트의 문제 해결 코어입니다.

매뉴얼을 기반으로 검색 가능한 형태를 만들고, 사용자 질문에 대해 맥락 있는 답변을 생성하는 흐름이 이 디렉터리에 모여 있습니다.

### `generate/`

생성형 미디어 확장 영역입니다.

텍스트 답변 이후의 경험까지 고려해, 이미지/영상 생성 파이프라인을 제품 기능으로 연결하려는 시도가 담겨 있습니다.

### `vision/`

실시간 입력이나 비전 관련 실험 흔적이 있는 영역입니다.

완성된 패키지라기보다, 향후 사용자 상황 인식과 연결될 수 있는 보조 축으로 읽는 것이 맞습니다.

## 실무진이 이 저장소에서 보면 좋은 포인트

- 단순한 CRUD 서버가 아니라는 점
- 모델 호출을 제품 흐름 안에 넣으려는 설계 의도가 있다는 점
- RAG, 생성형 미디어, 상태 저장, 앱 연동을 한 서비스 안에서 다뤘다는 점
- 프론트엔드와 분리된 백엔드 책임이 비교적 명확하다는 점

즉, 이 저장소는 "백엔드 개발을 했다"보다 "AI 기능이 들어간 사용자 지원 서비스를 어떻게 서버 구조로 풀었는가"를 보여주는 포트폴리오에 가깝습니다.

## 함께 봐야 하는 저장소

- `lgdx_frontend`

프론트엔드 저장소를 같이 보면 이 백엔드가 실제로 어떤 사용자 화면과 연결되는지 더 분명하게 보입니다.

두 저장소를 함께 보셨을 때, 제가 보여드리고 싶은 것은 개별 기술 하나가 아니라 "하나의 서비스 경험을 프론트와 백엔드로 나눠 설계하고 연결한 방식"입니다.
